div#tags_line
  - if (!tag_id) {
    span.label.label-primary.site-a-label
      a.current(href="/adm/posts/") Все записи
  - } else {
    span.label.label-default.site-a-label
      a.current(href="/adm/posts/") Все записи
  - }

  - each tag in tags_line
    - if(tag_id && tag_id == tag.tag_id)
        span.label.label-primary.site-a-label
          a.current(href="/adm/posts/#{tag.tag_id}/") #{tag.tag_name}
    - else
        span.label.label-default.site-a-label
          a(href="/adm/posts/#{tag.tag_id}/") #{tag.tag_name}
  div(class="clearfix")
